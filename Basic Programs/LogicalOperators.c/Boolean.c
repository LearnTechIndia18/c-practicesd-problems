#include <stdbool.h>
#include <stdio.h>
int main()

{
int p = 10, q = 4;
// instead of if and else statements
bool res = ((p == q) && printf("Chocolate"));
bool re1 = ((p != q) && printf("Chocolate"));
return 0;

}

